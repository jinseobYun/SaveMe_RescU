<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Diary.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar"/>
    <style name="Base.Theme.SaveMe" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item> <!-- 상태바 아이콘 색상 설정 (어두운 아이콘) -->
        <item name="android:windowTranslucentStatus">true</item>
    </style>

    <style name="Theme.SaveMe" parent="Base.Theme.SaveMe" />
</resources>